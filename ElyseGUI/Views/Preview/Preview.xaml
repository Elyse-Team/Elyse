<UserControl x:Class="ElyseGUI.Views.Preview.Preview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             xmlns:Preview="clr-namespace:ElyseGUI.Views.Preview"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Panel.ZIndex="1">
        <Grid.Background>
            <ImageBrush  ImageSource="/ElyseGUI;component/Images/bg.jpg"/>
        </Grid.Background>
        <Preview:Arrows VerticalAlignment="Center" Height="70" MinHeight="70"/>
        <Grid Background="Black" Opacity="0.7" Panel.ZIndex="2">
            <TextBlock VerticalAlignment="Center" TextAlignment="Center" Opacity="1" FontSize="50" Foreground="White"><Run Text="{StaticResource previewEmptyText}" Background="Black"/></TextBlock>
        </Grid>
    </Grid>
</UserControl>
